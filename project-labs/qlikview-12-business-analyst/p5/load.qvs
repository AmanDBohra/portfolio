// Guided storytelling & KPI polish — QlikView 12 Business Analyst (QVBA)
// Goal: Make dashboards executive-ready and easy to read.
// Concepts: Conditional coloring (traffic lights), Number formatting (num/money), Sparklines, Dynamic titles (GetCurrentSelections), Show conditions
// Qlik load script starter. Adapt connections/paths to your environment.

Policies:
LOAD
    PolicyID,
    Region,
    Product,
    WrittenPremium,
    IncurredLoss,
    Date(PolicyDate) as PolicyDate
FROM [lib://data/policies.qvd] (qvd);

// Example master calendar / incremental / set-analysis patterns go here.

// TODO: implement the full project:
// 1. Add traffic-light coloring vs targets and clean number formats.
// 2. Embed sparklines for trend context in tables.
// 3. Build dynamic titles from current selections.
// 4. Use show conditions to reveal detail when relevant.