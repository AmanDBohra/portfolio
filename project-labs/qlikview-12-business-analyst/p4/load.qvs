// Advanced charts: groups, running totals & trellis — QlikView 12 Business Analyst (QVBA)
// Goal: Add drill-down/cyclic navigation and cumulative analysis.
// Concepts: Drill-down vs cyclic groups, Above/Below & RangeSum, Dimensionality(), Trellis (small multiples), Aggr()
// Qlik load script starter. Adapt connections/paths to your environment.

Policies:
LOAD
    PolicyID,
    Region,
    Product,
    WrittenPremium,
    IncurredLoss,
    Date(PolicyDate) as PolicyDate
FROM [lib://data/policies.qvd] (qvd);

// Example master calendar / incremental / set-analysis patterns go here.

// TODO: implement the full project:
// 1. Add drill-down (Year>Quarter>Month) and cyclic groups.
// 2. Build running totals with RangeSum(Above(...)).
// 3. Vary subtotal logic with Dimensionality() in pivots.
// 4. Use trellis for side-by-side comparison.